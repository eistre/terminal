{{- if eq .Values.database.type "internal" }}
{{- if .Values.database.internal.image.pullPolicy }}
{{- include "terminal.validateEnum" (dict "value" .Values.database.internal.image.pullPolicy "allowed" (list "Always" "IfNotPresent" "Never") "name" "database.internal.image.pullPolicy") }}
{{- end }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "terminal.mariadb.fullname" . }}
  labels:
    {{- include "terminal.mariadb.labels" . | nindent 4 }}
spec:
  replicas: 1
  serviceName: {{ include "terminal.mariadb.fullname" . }}
  selector:
    matchLabels:
      {{- include "terminal.mariadb.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "terminal.mariadb.labels" . | nindent 8 }}
        {{- with .Values.database.internal.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.database.internal.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.database.internal.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.database.internal.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.database.internal.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.database.internal.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      automountServiceAccountToken: false
      containers:
        - name: mariadb
          image: "{{ .Values.database.internal.image.registry }}/{{ .Values.database.internal.image.repository }}:{{ .Values.database.internal.image.tag }}"
          imagePullPolicy: {{ include "terminal.imagePullPolicy" (dict "pullPolicy" .Values.database.internal.image.pullPolicy "tag" .Values.database.internal.image.tag) }}
          {{- with .Values.database.internal.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: mariadb
              containerPort: {{ .Values.database.port }}
              protocol: TCP
          env:
            {{- include "terminal.env.mariadb" . | nindent 12 }}
          volumeMounts:
            - name: mariadb-data
              mountPath: /var/lib/mysql
            - name: mariadb-run
              mountPath: /run/mysqld
            - name: mariadb-tmp
              mountPath: /tmp
          {{- with .Values.database.internal.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          livenessProbe:
            exec:
              command:
                - healthcheck.sh
                - --connect
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - healthcheck.sh
                - --connect
                - --innodb_initialized
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        {{- if not .Values.database.internal.persistence.enabled }}
        - name: mariadb-data
          emptyDir: {}
        {{- end }}
        - name: mariadb-run
          emptyDir: {}
        - name: mariadb-tmp
          emptyDir: {}
  {{- if .Values.database.internal.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: mariadb-data
        labels:
          {{- include "terminal.mariadb.labels" . | nindent 10 }}
      spec:
        accessModes:
          - {{ .Values.database.internal.persistence.accessMode }}
        {{- if eq .Values.database.internal.persistence.storageClass "-" }}
        storageClassName: ""
        {{- else if .Values.database.internal.persistence.storageClass }}
        storageClassName: {{ .Values.database.internal.persistence.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.database.internal.persistence.size | quote }}
  {{- end }}
{{- end }}
