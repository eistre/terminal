# Template to fill in (prod/long-lived)
# Usage:
#   Copy to terraform.prod.tfvars (or terraform.tfvars), replace all TODOs, then:
#     terraform plan  -var-file=terraform.prod.tfvars
#     terraform apply -var-file=terraform.prod.tfvars
# All required values are empty strings to prevent accidental use as-is.

# Required secrets
admin_email               = "" # TODO admin login email
admin_password            = "" # TODO admin password (min 8 chars)
auth_secret               = "" # TODO 32+ char secret, e.g., openssl rand -base64 32
mailer_sender             = "" # TODO verified SES sender email (leave blank to disable)

# Lightsail DB credentials
mysql_admin_password      = "" # TODO MySQL admin password (min 8 chars)
mysql_publicly_accessible = true

# Optional overrides (defaults used when left commented)
# resource_prefix                     = "terminal"
# aws_region                          = "eu-north-1"
# mailer_ses_region                   = "eu-north-1"
# image_registry                      = "ghcr.io"
# image_owner                         = "eistre"
# image_tag                           = "latest"
# lightsail_power                     = "nano"
# lightsail_scale                     = 1
# mysql_blueprint_id                  = "mysql_8_4"
# mysql_bundle_id                     = "micro_2_0"
# mysql_database_name                 = "terminal"
# mysql_admin_username                = "terminal"
# mysql_publicly_accessible           = false
# session_task_cpu                    = "256"
# session_task_memory                 = "512"
# session_cpu_architecture            = "X86_64"
# provisioner_container_expiry_minutes = 30
# logger_level                        = "info"
# user_expiry_days                    = 90
