{
  "name": "provisioner-cleanup",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "git clean -xdf .cache .turbo node_modules dist",
    "start": "node dist/index.js",
    "lint": "eslint . --cache --cache-location .cache/.eslintcache",
    "lint:fix": "eslint . --fix --cache --cache-location .cache/.eslintcache",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@terminal/env": "workspace:*",
    "@terminal/logger": "workspace:*",
    "@terminal/provisioner": "workspace:*"
  },
  "devDependencies": {
    "@terminal/eslint": "workspace:*",
    "@terminal/typescript": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:"
  }
}
