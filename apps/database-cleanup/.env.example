# Environment configuration for database cleanup

# General
NODE_ENV='production'  # Options: development, test, production
LOGGER_LEVEL='info'  # Options: error, warn, info, debug, trace

# Database
DATABASE_URL='mysql://terminal:password@localhost:3306/terminal'
DATABASE_SSL_ENABLED=  # Default: false
DATABASE_SSL_CA=  # Optional: path to CA pem file
DATABASE_SSL_CA_BASE64=  # Optional: base64-encoded CA pem
