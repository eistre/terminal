# Environment configuration for database cleanup

# General
NODE_ENV='production'  # Options: development, test, production
LOG_LEVEL='info'  # Options: error, warn, info, debug, trace

# Database
DATABASE_URL='mysql://terminal:password@localhost:3306/terminal'
